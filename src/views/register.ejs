<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
  <div class="register-container">
    <div class="container">
      <div class="row px-3 px-sm-0">
        <div class="content-left col-12 d-none col-sm-7 d-sm-block">
          <div class="brand">
            Pick n Click
          </div>
          <div class="detail">
            Access the entertaining world...
          </div>
        </div>
        <div class="content-right col-sm-5 col-12 green d-flex flex-column gap-3 py-3">
          <div class="brand d-sm-none">
            Duc
          </div>

          <form method="POST" action="/register">
            <div class="form-group">
              <label>Email:</label>
              <input class="form-control <% if (errors && !errors.isValidEmail) { %>is-invalid<% } %>" type="text" name="email" placeholder="Email address" value="<%= form?.email || '' %>">
            </div>

            <div class="form-group">
              <label>Username:</label>
              <input class="form-control" type="text" name="username" placeholder="Username" value="<%= form?.username || '' %>">
            </div>

            <div class="form-group">
              <label>Password:</label>
              <input class="form-control <% if (errors && !errors.isValidPassword) { %>is-invalid<% } %>" type="password" name="password" placeholder="Password">
            </div>

            <div class="form-group">
              <label>Re-enter password:</label>
              <input class="form-control <% if (errors && !errors.isValidConfirmPass) { %>is-invalid<% } %>" type="password" name="confirmPass" placeholder="Re-enter your password">
            </div>

            <button class="btn btn-primary mt-2" type="submit">Register</button>
          </form>

          <hr />
          <div class="text-center">
            <a href="/login" class="btn btn-success">Already have your account? Login!</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
